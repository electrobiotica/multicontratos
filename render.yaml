services:
  - type: web
    name: analizador-legal
    env: python
    buildCommand: "./build.sh && pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
